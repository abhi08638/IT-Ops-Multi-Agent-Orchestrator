# This project's virtual environment lives at the repo root (created via
# `python -m venv .`), so its internals are ignored individually rather
# than via a blanket venv .gitignore.
Include/
Lib/
Scripts/
etc/
share/
pyvenv.cfg
__pycache__/
*.pyc
.pytest_cache/

# Generated at runtime — not committed
mcp_server/tickets.db
mcp_server/remediation_log.db
orchestrator/approvals.db
orchestrator/incidents.db
orchestrator/checkpoints.db

# Secrets
.env

# Local Claude Code settings (machine-specific, not project config)
.claude/settings.local.json

# Editor / OS cruft
.vscode/
.idea/
*.swp
Thumbs.db
.DS_Store
